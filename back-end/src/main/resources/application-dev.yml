spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/h2/nuxt-db/nuxt
    username: sa
    password: ''
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
